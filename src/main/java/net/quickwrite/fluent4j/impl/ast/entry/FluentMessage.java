package net.quickwrite.fluent4j.impl.ast.entry;

import net.quickwrite.fluent4j.ast.FluentEntry;

public class FluentMessage extends FluentEntryBase implements FluentEntry {
    public FluentMessage(final String identifier) {
        super(identifier);
    }
}
